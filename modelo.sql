CREATE TABLE `agudos`.<tabela> (
  `id` INT NOT NULL AUTO_INCREMENT,
  `created` DATETIME NULL,
  `created_by` INT NULL,
  `updated` DATETIME NULL,
  `updated_by` INT NULL,
  `deleted` DATETIME NULL,
  `deleted_by` INT NULL,
  PRIMARY KEY (`id`));